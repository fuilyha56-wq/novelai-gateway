我已经把代码路径摸清了：

- `config/models.toml` 里，V4.5/V5 的 full / curated / inpaint 全部都已公开；`-limit` 只是同一模型的安全别名。
- `src/proxy/openai.py` 里，`/v1/images/inpainting`、`/v1/images/edits`、`/v1/images/img2img`、`/v1/images/vibe-transfer` 都走同一套图像上游管线；full 模型本身没有被代码硬禁用。
- 现阶段最可疑的不是“模型不存在”，而是：
  1) 运行时模型注册表和仓库配置不一致；
  2) redraw 请求的字段在某个路径里被错误吸收/丢失；
  3) 某个 `-limit` / 额度门控误伤了 full 模型的 redraw 路径；
  4) 用户反馈指的是“模型在 `/v1/models` 里看不到”而不是“请求失败”。

我准备按下面的最小修复路线做：

1. 先做一轮只读核查，确认服务器运行时的 `/v1/models`、`models.toml`、以及 `openai.py` 的 redraw 路由是否一致，重点看 full V4.5/V5 的 inpaint / edits / img2img 是否都在列表里、是否被错误归入 `-limit`。
2. 先补/跑针对性回归：
   - full V4.5 / V5 的 `edits`、`inpainting`、`img2img`、`vibe-transfer`
   - `-limit` 的边界拒绝
   - `mask` / `image` / `extra_params` 透传
   - `/v1/models` 模型曝光
3. 如果复现出问题，只改最小范围：
   - 若是模型曝光问题，就修模型注册/建议模型生成；
   - 若是 redraw 请求被误拦，就修 `openai.py` 的门控或参数合并；
   - 若是运行时配置不一致，就同步服务器配置。
4. 回归测试通过后再部署到服务器，并补一次日志级 smoke test。

如果你同意，我下一步就开始执行这套检查和修复。